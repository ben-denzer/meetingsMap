import styled from 'styled-components';
import { filterHeight } from '../../config/styleConfig';

export const MapFilterBarWrapper = styled.div`
  height: ${filterHeight}px;
  display: flex;
  align-items: center;
  padding: 0 15px;
`;
