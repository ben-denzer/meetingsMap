import styled from 'styled-components';

export const PopupMeetingDetailsWrapper = styled.div`
  border: 1px solid #222;
  margin: 3px 5px;
  padding: 2px;

  .heading {
    font-size: 12px;
    font-weight: bold;
  }

  .day {
    font-weight: bold;
  }

  .time {
    font-weight: bold;
  }

  .meetingType {
    font-style: italic;
  }

  .openOrClosed {
    font-style: italic;
  }
`;
